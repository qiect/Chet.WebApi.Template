# 基于 .Net 8 和 EF Core ORM 的 WebApi 模板框架设计

## 1. 架构设计原则
- **模块化设计**：将应用程序分解为可独立部署的模块
- **依赖注入**：使用 .Net 内置的依赖注入容器
- **仓储模式**：抽象数据访问层，便于测试和切换数据库
- **服务层**：实现业务逻辑
- **API 层**：处理 HTTP 请求和响应
- **配置中心**：支持不同环境的配置管理
- **日志系统**：集成 Serilog 日志框架
- **缓存系统**：集成 Redis 缓存
- **对象映射**：使用 AutoMapper 实现对象映射
- **身份验证和授权**：使用 JWT 实现
- **异常处理**：统一的异常处理机制
- **健康检查**：支持微服务的健康检查
- **API 文档**：集成 Swagger

## 2. 单体服务设计

### 2.1 单体服务定义
单体服务是将所有功能模块打包在一个应用程序中，共享同一个数据库和配置，部署在一个服务器上。

### 2.2 单体服务项目结构

```
├── Chet.WebApi.Template.sln          # 解决方案文件
├── Chet.WebApi.Template.Core          # 核心层
│   ├── Chet.WebApi.Template.Domain     # 领域模型
│   │   ├── User.cs                      # 用户实体
│   │   └── BaseEntity.cs                # 基础实体
│   ├── Chet.WebApi.Template.Contracts  # 接口定义
│   │   ├── IRepository.cs               # 通用仓储接口
│   │   ├── IUserRepository.cs           # 用户仓储接口
│   │   ├── IUserService.cs              # 用户服务接口
│   │   └── IJwtService.cs               # JWT 服务接口
│   └── Chet.WebApi.Template.Shared     # 公共工具类
│       ├── ErrorResponse.cs             # 错误响应模型
│       └── ExceptionHelper.cs           # 异常处理工具
├── Chet.WebApi.Template.Infrastructure # 基础设施层
│   ├── Chet.WebApi.Template.Data       # 数据访问（EF Core 实现）
│   │   ├── AppDbContext.cs              # 数据库上下文
│   │   ├── EfCoreRepository.cs          # 通用仓储实现
│   │   └── UserRepository.cs            # 用户仓储实现
│   ├── Chet.WebApi.Template.Logging    # Serilog 日志实现
│   │   └── SerilogExtensions.cs         # Serilog 扩展
│   ├── Chet.WebApi.Template.Caching    # Redis 缓存实现
│   │   └── RedisCacheService.cs         # Redis 缓存服务
│   └── Chet.WebApi.Template.Configuration # 配置管理
│       └── AppSettings.cs               # 应用配置模型
├── Chet.WebApi.Template.Application    # 应用层
│   ├── Chet.WebApi.Template.Services   # 业务服务
│   │   ├── UserService.cs               # 用户服务实现
│   │   ├── JwtService.cs                # JWT 服务实现
│   │   └── AuthService.cs               # 认证服务实现
│   ├── Chet.WebApi.Template.DTOs       # 数据传输对象
│   │   ├── UserDto.cs                   # 用户 DTO
│   │   ├── UserCreateDto.cs             # 用户创建 DTO
│   │   ├── UserUpdateDto.cs             # 用户更新 DTO
│   │   ├── LoginDto.cs                  # 登录 DTO
│   │   ├── RegisterDto.cs               # 注册 DTO
│   │   └── JwtTokenDto.cs               # JWT 令牌 DTO
│   └── Chet.WebApi.Template.Mapping    # AutoMapper 配置
│       └── MappingProfile.cs            # 映射配置文件
├── Chet.WebApi.Template.Api            # API 层
│   ├── Chet.WebApi.Template.Controllers # 控制器
│   │   ├── UsersController.cs           # 用户控制器
│   │   └── AuthController.cs            # 认证控制器
│   ├── Chet.WebApi.Template.Middleware  # 中间件
│   │   └── ErrorHandlingMiddleware.cs   # 异常处理中间件
│   ├── Program.cs                       # 应用入口
│   ├── appsettings.json                 # 应用配置
│   └── appsettings.Development.json     # 开发环境配置
└── Chet.WebApi.Template.Tests          # 测试层
    ├── Chet.WebApi.Template.UnitTests   # 单元测试
    └── Chet.WebApi.Template.IntegrationTests # 集成测试
```

### 2.3 单体服务配置

#### 2.3.1 appsettings.json
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=ChetWebApiTemplate;Trusted_Connection=True;MultipleActiveResultSets=true",
    "Redis": "localhost:6379"
  },
  "Jwt": {
    "Key": "YourSuperSecretKeyForAuthentication",
    "Issuer": "Chet.WebApi.Template",
    "Audience": "Chet.WebApi.Template.Client",
    "AccessTokenExpirationMinutes": 30,
    "RefreshTokenExpirationDays": 7
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    },
    "WriteTo": [
      {
        "Name": "Console",
        "Args": {
          "theme": "Serilog.Sinks.SystemConsole.Themes.AnsiConsoleTheme::Code, Serilog.Sinks.Console",
          "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} [{Level:u3}] {Message:lj}{NewLine}{Exception}"
        }
      },
      {
        "Name": "File",
        "Args": {
          "path": "./logs/log-.txt",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 7,
          "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} [{Level:u3}] {Message:lj}{NewLine}{Exception}"
        }
      }
    ]
  }
}
```

### 2.4 单体服务部署

#### 2.4.1 部署方式
- 使用 IIS 或 Kestrel 直接部署
- 使用 Docker 容器部署
- 支持 Azure App Service 或 AWS EC2 部署

#### 2.4.2 Dockerfile
```dockerfile
FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS base
WORKDIR /app
EXPOSE 80
EXPOSE 443

FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY ["Chet.WebApi.Template.Api/Chet.WebApi.Template.Api.csproj", "Chet.WebApi.Template.Api/"]
COPY ["Chet.WebApi.Template.Application/Chet.WebApi.Template.Application.csproj", "Chet.WebApi.Template.Application/"]
COPY ["Chet.WebApi.Template.Infrastructure/Chet.WebApi.Template.Infrastructure.csproj", "Chet.WebApi.Template.Infrastructure/"]
COPY ["Chet.WebApi.Template.Core/Chet.WebApi.Template.Core.csproj", "Chet.WebApi.Template.Core/"]
RUN dotnet restore "Chet.WebApi.Template.Api/Chet.WebApi.Template.Api.csproj"
COPY . .
WORKDIR "/src/Chet.WebApi.Template.Api"
RUN dotnet build "Chet.WebApi.Template.Api.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "Chet.WebApi.Template.Api.csproj" -c Release -o /app/publish /p:UseAppHost=false

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "Chet.WebApi.Template.Api.dll"]
```

## 3. 微服务设计

### 3.1 微服务定义
微服务是将应用程序拆分为多个独立的服务，每个服务负责一个特定的业务领域，有自己的数据库和配置，可以独立部署和扩展。

### 3.2 微服务项目结构

```
├── services/                           # 服务目录
│   ├── user-service/                   # 用户服务
│   │   ├── UserService.sln             # 用户服务解决方案
│   │   ├── src/                        # 源代码
│   │   │   ├── UserService.Core        # 核心层
│   │   │   ├── UserService.Infrastructure # 基础设施层
│   │   │   ├── UserService.Application # 应用层
│   │   │   └── UserService.Api         # API 层
│   │   ├── tests/                      # 测试
│   │   ├── Dockerfile                  # Docker 配置
│   │   └── docker-compose.yml          # Docker Compose 配置
│   ├── order-service/                  # 订单服务
│   │   ├── OrderService.sln            # 订单服务解决方案
│   │   ├── src/                        # 源代码
│   │   │   ├── OrderService.Core       # 核心层
│   │   │   ├── OrderService.Infrastructure # 基础设施层
│   │   │   ├── OrderService.Application # 应用层
│   │   │   └── OrderService.Api        # API 层
│   │   ├── tests/                      # 测试
│   │   ├── Dockerfile                  # Docker 配置
│   │   └── docker-compose.yml          # Docker Compose 配置
│   └── product-service/                # 产品服务
│       ├── ProductService.sln          # 产品服务解决方案
│       ├── src/                        # 源代码
│       │   ├── ProductService.Core     # 核心层
│       │   ├── ProductService.Infrastructure # 基础设施层
│       │   ├── ProductService.Application # 应用层
│       │   └── ProductService.Api      # API 层
│       ├── tests/                      # 测试
│       ├── Dockerfile                  # Docker 配置
│       └── docker-compose.yml          # Docker Compose 配置
├── gateway/                            # API 网关
│   ├── OcelotApiGateway.sln            # API 网关解决方案
│   ├── src/                            # 源代码
│   │   ├── OcelotApiGateway            # 网关项目
│   │   └── OcelotApiGateway.Configuration # 网关配置
│   ├── Dockerfile                      # Docker 配置
│   └── docker-compose.yml              # Docker Compose 配置
├── common/                             # 公共库
│   ├── Chet.Common.sln                 # 公共库解决方案
│   ├── src/                            # 源代码
│   │   ├── Chet.Common.Core            # 核心公共库
│   │   ├── Chet.Common.Infrastructure  # 基础设施公共库
│   │   └── Chet.Common.Application     # 应用公共库
│   └── tests/                          # 测试
└── docker-compose.yml                  # 全局 Docker Compose 配置
```

### 3.3 微服务配置

#### 3.3.1 用户服务 appsettings.json
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=sqlserver;Database=UserService;User Id=sa;Password=YourStrong!Passw0rd;",
    "Redis": "redis:6379"
  },
  "Jwt": {
    "Key": "YourSuperSecretKeyForAuthentication",
    "Issuer": "Chet.UserService",
    "Audience": "Chet.UserService.Client",
    "AccessTokenExpirationMinutes": 30,
    "RefreshTokenExpirationDays": 7
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    },
    "WriteTo": [
      {
        "Name": "Console",
        "Args": {
          "theme": "Serilog.Sinks.SystemConsole.Themes.AnsiConsoleTheme::Code, Serilog.Sinks.Console",
          "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} [{Level:u3}] {Message:lj}{NewLine}{Exception}"
        }
      }
    ]
  },
  "ServiceDiscovery": {
    "Host": "consul",
    "Port": 8500,
    "ServiceName": "UserService",
    "ServiceAddress": "http://userservice:80",
    "HealthCheckPath": "/health",
    "HealthCheckInterval": 10
  }
}
```

#### 3.3.2 API 网关配置 (ocelot.json)
```json
{
  "Routes": [
    {
      "DownstreamPathTemplate": "/api/{everything}",
      "DownstreamScheme": "http",
      "DownstreamHostAndPorts": [
        {
          "Host": "userservice",
          "Port": 80
        }
      ],
      "UpstreamPathTemplate": "/api/users/{everything}",
      "UpstreamHttpMethod": [ "Get", "Post", "Put", "Delete" ],
      "AuthenticationOptions": {
        "AuthenticationProviderKey": "Bearer",
        "AllowedScopes": []
      }
    },
    {
      "DownstreamPathTemplate": "/api/{everything}",
      "DownstreamScheme": "http",
      "DownstreamHostAndPorts": [
        {
          "Host": "orderservice",
          "Port": 80
        }
      ],
      "UpstreamPathTemplate": "/api/orders/{everything}",
      "UpstreamHttpMethod": [ "Get", "Post", "Put", "Delete" ],
      "AuthenticationOptions": {
        "AuthenticationProviderKey": "Bearer",
        "AllowedScopes": []
      }
    },
    {
      "DownstreamPathTemplate": "/api/{everything}",
      "DownstreamScheme": "http",
      "DownstreamHostAndPorts": [
        {
          "Host": "productservice",
          "Port": 80
        }
      ],
      "UpstreamPathTemplate": "/api/products/{everything}",
      "UpstreamHttpMethod": [ "Get", "Post", "Put", "Delete" ],
      "AuthenticationOptions": {
        "AuthenticationProviderKey": "Bearer",
        "AllowedScopes": []
      }
    }
  ],
  "GlobalConfiguration": {
    "BaseUrl": "http://localhost:5000",
    "ServiceDiscoveryProvider": {
      "Host": "consul",
      "Port": 8500,
      "Type": "Consul"
    }
  }
}
```

#### 3.3.3 Docker Compose 配置 (docker-compose.yml)
```yaml
version: '3.8'

services:
  # API Gateway
  gateway:
    build: ./gateway
    ports:
      - "5000:80"
    depends_on:
      - consul
      - userservice
      - orderservice
      - productservice
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  # User Service
  userservice:
    build: ./services/user-service
    ports:
      - "5001:80"
    depends_on:
      - sqlserver
      - redis
      - consul
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=UserService;User Id=sa;Password=YourStrong!Passw0rd;
      - ConnectionStrings__Redis=redis:6379

  # Order Service
  orderservice:
    build: ./services/order-service
    ports:
      - "5002:80"
    depends_on:
      - sqlserver
      - redis
      - consul
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=OrderService;User Id=sa;Password=YourStrong!Passw0rd;
      - ConnectionStrings__Redis=redis:6379

  # Product Service
  productservice:
    build: ./services/product-service
    ports:
      - "5003:80"
    depends_on:
      - sqlserver
      - redis
      - consul
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=ProductService;User Id=sa;Password=YourStrong!Passw0rd;
      - ConnectionStrings__Redis=redis:6379

  # Database
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong!Passw0rd

  # Redis
  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  # Service Discovery
  consul:
    image: consul:latest
    ports:
      - "8500:8500"
    command: consul agent -dev -client=0.0.0.0
```

### 3.4 微服务服务发现与注册

#### 3.4.1 Consul 服务注册
```csharp
// 在 Program.cs 中添加

// 注册 Consul 服务
builder.Services.AddSingleton<IConsulClient, ConsulClient>(config =>
{
    config.Address = new Uri($"http://{builder.Configuration["ServiceDiscovery:Host"]}:{builder.Configuration["ServiceDiscovery:Port"]}");
});

var app = builder.Build();

// 注册服务到 Consul
using (var scope = app.Services.CreateScope())
{
    var consulClient = scope.ServiceProvider.GetRequiredService<IConsulClient>();
    var configuration = scope.ServiceProvider.GetRequiredService<IConfiguration>();
    
    var serviceName = configuration["ServiceDiscovery:ServiceName"];
    var serviceAddress = configuration["ServiceDiscovery:ServiceAddress"];
    var healthCheckPath = configuration["ServiceDiscovery:HealthCheckPath"];
    var healthCheckInterval = int.Parse(configuration["ServiceDiscovery:HealthCheckInterval"]);
    
    var registration = new AgentServiceRegistration
    {
        ID = $"{serviceName}-{Guid.NewGuid()}",
        Name = serviceName,
        Address = serviceAddress.Split('//')[1].Split(':')[0],
        Port = int.Parse(serviceAddress.Split(':')[2]),
        Check = new AgentServiceCheck
        {
            HTTP = $"{serviceAddress}{healthCheckPath}",
            Interval = TimeSpan.FromSeconds(healthCheckInterval),
            Timeout = TimeSpan.FromSeconds(5)
        }
    };
    
    await consulClient.Agent.ServiceDeregister(registration.ID);
    await consulClient.Agent.ServiceRegister(registration);
}
```

### 3.5 微服务间通信

#### 3.5.1 HTTP 客户端配置
```csharp
// 在 Program.cs 中添加

builder.Services.AddHttpClient();
builder.Services.AddScoped<IHttpClientFactory, HttpClientFactory>;
```

#### 3.5.2 服务间调用示例
```csharp
public class OrderService : IOrderService
{
    private readonly IHttpClientFactory _httpClientFactory;
    private readonly IConfiguration _configuration;
    private readonly ILogger<OrderService> _logger;
    
    public OrderService(IHttpClientFactory httpClientFactory, IConfiguration configuration, ILogger<OrderService> logger)
    {
        _httpClientFactory = httpClientFactory;
        _configuration = configuration;
        _logger = logger;
    }
    
    public async Task<OrderDto> CreateOrderAsync(OrderCreateDto orderCreateDto)
    {
        // 调用产品服务获取产品信息
        var httpClient = _httpClientFactory.CreateClient();
        var productResponse = await httpClient.GetAsync($"{_configuration["Services:ProductService"]}/api/products/{orderCreateDto.ProductId}");
        
        if (!productResponse.IsSuccessStatusCode)
        {
            throw new BadRequestException("Product not found");
        }
        
        var product = await productResponse.Content.ReadFromJsonAsync<ProductDto>();
        
        // 创建订单逻辑
        var order = new Order
        {
            ProductId = orderCreateDto.ProductId,
            ProductName = product.Name,
            Price = product.Price,
            Quantity = orderCreateDto.Quantity,
            TotalAmount = product.Price * orderCreateDto.Quantity,
            UserId = orderCreateDto.UserId,
            Status = OrderStatus.Pending
        };
        
        await _orderRepository.AddAsync(order);
        
        return _mapper.Map<OrderDto>(order);
    }
}
```

## 4. 单体服务 VS 微服务对比

| 特性 | 单体服务 | 微服务 |
|------|----------|--------|
| **项目结构** | 单一解决方案，所有模块在一起 | 多个独立解决方案，每个服务一个 |
| **数据库** | 共享数据库 | 每个服务有自己的数据库（或共享数据库） |
| **配置管理** | 单一配置文件 | 分布式配置中心 |
| **部署方式** | 单一应用部署 | 独立容器部署 |
| **通信方式** | 内部方法调用 | REST API 或消息队列 |
| **扩展方式** | 垂直扩展 | 水平扩展 |
| **故障隔离** | 单点故障影响整个应用 | 单个服务故障不影响其他服务 |
| **开发速度** | 初期开发速度快 | 初期开发速度慢，长期维护方便 |
| **技术栈** | 统一技术栈 | 每个服务可以使用不同技术栈 |
| **团队协作** | 适合小团队 | 适合大团队，每个团队负责一个服务 |
| **测试复杂度** | 测试简单 | 测试复杂，需要集成测试 |
| **部署复杂度** | 部署简单 | 部署复杂，需要容器编排和服务发现 |

## 5. 关键功能实现

### 5.1 JWT 身份验证
```csharp
public interface IJwtService
{
    string GenerateAccessToken(User user);
    string GenerateRefreshToken();
    ClaimsPrincipal GetPrincipalFromExpiredToken(string token);
    Task<JwtTokenDto> RefreshTokenAsync(string accessToken, string refreshToken);
}

public class JwtService : IJwtService
{
    private readonly IConfiguration _configuration;
    private readonly IUserRepository _userRepository;
    private readonly ILogger<JwtService> _logger;

    public JwtService(IConfiguration configuration, IUserRepository userRepository, ILogger<JwtService> logger)
    {
        _configuration = configuration;
        _userRepository = userRepository;
        _logger = logger;
    }

    public string GenerateAccessToken(User user)
    {
        var claims = new[]
        {
            new Claim(JwtRegisteredClaimNames.Sub, user.Id.ToString()),
            new Claim(JwtRegisteredClaimNames.Email, user.Email),
            new Claim(JwtRegisteredClaimNames.Jti, Guid.NewGuid().ToString())
        };

        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration["Jwt:Key"]));
        var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);

        var token = new JwtSecurityToken(
            issuer: _configuration["Jwt:Issuer"],
            audience: _configuration["Jwt:Audience"],
            claims: claims,
            expires: DateTime.Now.AddMinutes(Convert.ToDouble(_configuration["Jwt:AccessTokenExpirationMinutes"])),
            signingCredentials: creds);

        return new JwtSecurityTokenHandler().WriteToken(token);
    }

    public string GenerateRefreshToken()
    {
        var randomNumber = new byte[32];
        using (var rng = RandomNumberGenerator.Create())
        {
            rng.GetBytes(randomNumber);
            return Convert.ToBase64String(randomNumber);
        }
    }

    public ClaimsPrincipal GetPrincipalFromExpiredToken(string token)
    {
        var tokenValidationParameters = new TokenValidationParameters
        {
            ValidateAudience = false,
            ValidateIssuer = false,
            ValidateIssuerSigningKey = true,
            IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration["Jwt:Key"])),
            ValidateLifetime = false
        };

        var tokenHandler = new JwtSecurityTokenHandler();
        var principal = tokenHandler.ValidateToken(token, tokenValidationParameters, out SecurityToken securityToken);
        if (securityToken is not JwtSecurityToken jwtSecurityToken || !jwtSecurityToken.Header.Alg.Equals(SecurityAlgorithms.HmacSha256, StringComparison.InvariantCultureIgnoreCase))
        {
            throw new SecurityTokenException("Invalid token");
        }

        return principal;
    }

    public async Task<JwtTokenDto> RefreshTokenAsync(string accessToken, string refreshToken)
    {
        var principal = GetPrincipalFromExpiredToken(accessToken);
        var userId = Convert.ToInt32(principal.FindFirst(JwtRegisteredClaimNames.Sub)?.Value);
        var user = await _userRepository.GetByIdAsync(userId);

        if (user == null || user.RefreshToken != refreshToken || user.RefreshTokenExpiryTime < DateTime.Now)
        {
            throw new SecurityTokenException("Invalid refresh token");
        }

        var newAccessToken = GenerateAccessToken(user);
        var newRefreshToken = GenerateRefreshToken();

        user.RefreshToken = newRefreshToken;
        user.RefreshTokenExpiryTime = DateTime.Now.AddDays(Convert.ToDouble(_configuration["Jwt:RefreshTokenExpirationDays"]));
        await _userRepository.UpdateAsync(user);

        return new JwtTokenDto
        {
            AccessToken = newAccessToken,
            RefreshToken = newRefreshToken
        };
    }
}
```

### 5.2 Redis 缓存服务
```csharp
public interface ICacheService
{
    Task<T> GetAsync<T>(string key);
    Task SetAsync<T>(string key, T value, TimeSpan? expiry = null);
    Task RemoveAsync(string key);
    Task<bool> ExistsAsync(string key);
    Task<T> GetOrCreateAsync<T>(string key, Func<Task<T>> factory, TimeSpan? expiry = null);
}

public class RedisCacheService : ICacheService
{
    private readonly IDatabase _database;
    private readonly ILogger<RedisCacheService> _logger;

    public RedisCacheService(IConnectionMultiplexer connectionMultiplexer, ILogger<RedisCacheService> logger)
    {
        _database = connectionMultiplexer.GetDatabase();
        _logger = logger;
    }

    public async Task<T> GetAsync<T>(string key)
    {
        try
        {
            var value = await _database.StringGetAsync(key);
            if (value.IsNull)
            {
                return default;
            }
            return JsonSerializer.Deserialize<T>(value);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error getting value from Redis cache for key: {Key}", key);
            return default;
        }
    }

    public async Task SetAsync<T>(string key, T value, TimeSpan? expiry = null)
    {
        try
        {
            var json = JsonSerializer.Serialize(value);
            await _database.StringSetAsync(key, json, expiry);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error setting value to Redis cache for key: {Key}", key);
        }
    }

    public async Task RemoveAsync(string key)
    {
        try
        {
            await _database.KeyDeleteAsync(key);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error removing value from Redis cache for key: {Key}", key);
        }
    }

    public async Task<bool> ExistsAsync(string key)
    {
        try
        {
            return await _database.KeyExistsAsync(key);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error checking if key exists in Redis cache: {Key}", key);
            return false;
        }
    }

    public async Task<T> GetOrCreateAsync<T>(string key, Func<Task<T>> factory, TimeSpan? expiry = null)
    {
        var value = await GetAsync<T>(key);
        if (value != null)
        {
            return value;
        }

        value = await factory();
        if (value != null)
        {
            await SetAsync(key, value, expiry);
        }

        return value;
    }
}
```

### 5.3 AutoMapper 配置
```csharp
public class MappingProfile : Profile
{
    public MappingProfile()
    {
        CreateMap<User, UserDto>();
        CreateMap<UserCreateDto, User>();
        CreateMap<UserUpdateDto, User>();
        CreateMap<RegisterDto, User>();
        
        // 添加更多映射配置
    }
}
```

## 6. 技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| **框架** | .Net | 8.0 |
| **ORM** | Entity Framework Core | 8.0 |
| **缓存** | Redis + StackExchange.Redis | 2.6.122 |
| **对象映射** | AutoMapper | 12.0.1 |
| **日志** | Serilog | 3.1.1 |
| **身份验证** | JWT | 6.32.0 |
| **API 文档** | Swagger | 6.4.0 |
| **API 网关** | Ocelot | 23.0.0 |
| **服务发现** | Consul | 1.87.0 |
| **容器化** | Docker | 24.0.0 |
| **CI/CD** | GitHub Actions | - |

## 7. 部署建议

### 7.1 单体服务部署
- **开发环境**：直接在本地运行
- **测试环境**：部署到测试服务器，使用 IIS 或 Kestrel
- **生产环境**：部署到生产服务器，使用 IIS 或 Docker 容器，配置负载均衡

### 7.2 微服务部署
- **开发环境**：使用 Docker Compose 本地运行所有服务
- **测试环境**：部署到 Kubernetes 集群，使用 Helm 管理
- **生产环境**：部署到 Kubernetes 集群，配置水平扩展、自动缩放、监控和日志收集

## 8. 监控与日志

### 8.1 监控
- **Prometheus**：监控服务指标
- **Grafana**：可视化监控数据
- **Jaeger**：分布式追踪

### 8.2 日志收集
- **ELK Stack**：Elasticsearch + Logstash + Kibana
- **Graylog**：集中式日志管理

## 9. 安全性考虑

- **HTTPS**：所有 API 都使用 HTTPS
- **JWT 令牌**：使用强密钥，设置合理的过期时间
- **输入验证**：所有输入都进行验证
- **SQL 注入防护**：使用 EF Core 参数化查询
- **跨域资源共享 (CORS)**：配置合理的 CORS 策略
- **API 速率限制**：防止 API 滥用
- **敏感数据加密**：数据库中敏感数据加密存储

## 10. 总结

本设计方案提供了一个基于 .Net 8 和 EF Core ORM 的 WebApi 模板框架，支持单体服务和微服务两种架构模式。单体服务适合小型应用，开发速度快，部署简单；微服务适合大型应用，具有更好的扩展性和故障隔离性。

框架集成了 JWT 身份验证、Redis 缓存、AutoMapper 对象映射和 Serilog 日志系统，提供了完整的 WebApi 开发基础架构。同时，框架支持 API 版本控制、健康检查和 Swagger 文档，便于开发和维护。

通过本框架，开发者可以快速搭建 WebApi 应用，根据业务需求选择合适的架构模式，并在此基础上进行扩展和定制。