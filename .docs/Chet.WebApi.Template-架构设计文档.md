# 架构设计文档

## 1. 文档概述

本文档详细描述了Chet.WebApi.Template项目的架构设计，包括分层架构、各层职责、组件关系以及依赖引用规则。本架构采用经典的分层设计，旨在实现高内聚、低耦合，便于维护和扩展。

## 2. 架构目标

- **解耦**: 各层之间通过依赖注入实现松耦合
- **可测试性**: 便于编写单元测试和集成测试
- **可扩展性**: 支持单体服务和微服务架构
- **可维护性**: 清晰的分层结构便于理解和维护
- **可重用性**: 核心业务逻辑可在不同部署环境中重用

## 3. 分层架构详解

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API Layer (表示层)                            │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      Controllers, Middleware, Program.cs             │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↑
                                      |
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Application Layer (应用层)                         │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────┐  │
│  │         DTOs            │  │        Mapping         │  │   Services   │  │
│  └─────────────────────────┘  └─────────────────────────┘  └─────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↑
                                      |
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Core Layer (核心层)                            │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────┐  │
│  │         Domain          │  │        Contracts       │  │    Shared    │  │
│  └─────────────────────────┘  └─────────────────────────┘  └─────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↑
                                      |
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Infrastructure Layer (基础设施层)                     │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────┐  │
│  │          Data           │  │        Caching         │  │ Configuration│  │
│  └─────────────────────────┘  └─────────────────────────┘  └─────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                              Logging                                 │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↑
                                      |
┌─────────────────────────────────────────────────────────────────────────────┐
│                               External Systems                              │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐  │
│  │    Database     │  │      Redis      │  │       Other Services        │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 核心层 (Core Layer)

**职责**: 包含领域模型、接口定义和共享类，是整个应用的核心，不依赖任何其他层。

#### 3.2.1 Domain子层

**职责**: 定义领域模型和业务实体，体现业务核心概念。

**组件**: 
- `BaseEntity.cs`: 所有实体的基类，包含通用字段如Id、CreatedAt、UpdatedAt
- `User.cs`: 用户实体，包含用户的基本信息

**特点**: 
- 只包含实体定义，不包含业务逻辑
- 实体之间的关系通过导航属性定义
- 遵循领域驱动设计原则

#### 3.2.2 Contracts子层

**职责**: 定义所有业务接口，包括仓储接口、服务接口等。

**组件**: 
- `IRepository<T>`: 通用仓储接口，定义CRUD操作
- `IUserRepository`: 用户仓储接口，扩展通用仓储接口
- `IUserService`: 用户服务接口，定义用户相关业务逻辑
- `IAuthService`: 认证服务接口，定义认证相关业务逻辑
- `IJwtService`: JWT服务接口，定义JWT令牌相关操作
- `ICacheService`: 缓存服务接口，定义缓存操作

**特点**: 
- 只定义接口，不包含实现
- 接口设计遵循单一职责原则
- 便于测试和替换实现

#### 3.2.3 Shared子层

**职责**: 包含各层共享的类和异常定义。

**组件**: 
- `BadRequestException.cs`: 自定义400错误异常
- `NotFoundException.cs`: 自定义404错误异常
- `ErrorResponse.cs`: API错误响应类

**特点**: 
- 包含跨层使用的共享类型
- 定义应用中使用的自定义异常
- 提供统一的错误响应格式

### 3.3 应用层 (Application Layer)

**职责**: 实现业务逻辑，协调领域模型和基础设施，依赖Core层。

#### 3.3.1 DTOs子层

**职责**: 定义数据传输对象，用于层间数据传输。

**组件**: 
- `UserDto.cs`: 用户数据传输对象
- `UserCreateDto.cs`: 用户创建请求对象
- `UserUpdateDto.cs`: 用户更新请求对象
- `LoginDto.cs`: 登录请求对象
- `RegisterDto.cs`: 注册请求对象
- `JwtTokenDto.cs`: JWT令牌响应对象
- `RefreshTokenDto.cs`: 刷新令牌请求对象

**特点**: 
- 用于API请求和响应
- 与数据库实体解耦
- 便于验证和转换

#### 3.3.2 Mapping子层

**职责**: 定义领域模型和DTO之间的映射关系。

**组件**: 
- `MappingProfile.cs`: AutoMapper配置类，定义映射规则

**特点**: 
- 使用AutoMapper实现对象映射
- 集中管理映射规则
- 减少手动映射代码

#### 3.3.3 Services子层

**职责**: 实现业务逻辑，调用仓储和其他服务。

**组件**: 
- `UserService.cs`: 实现IUserService接口，处理用户相关业务逻辑
- `AuthService.cs`: 实现IAuthService接口，处理认证相关业务逻辑
- `JwtService.cs`: 实现IJwtService接口，处理JWT令牌生成和验证

**特点**: 
- 实现业务逻辑，不直接访问数据库
- 通过接口调用仓储和其他服务
- 包含事务和业务规则验证

### 3.4 基础设施层 (Infrastructure Layer)

**职责**: 实现基础设施服务，如数据库访问、缓存、配置和日志，依赖Core层和Application层。

#### 3.4.1 Data子层

**职责**: 实现数据访问，包括数据库上下文和仓储实现。

**组件**: 
- `AppDbContext.cs`: EF Core数据库上下文，定义实体映射
- `EfCoreRepository.cs`: 通用仓储实现，基于EF Core
- `UserRepository.cs`: 用户仓储实现，扩展通用仓储

**特点**: 
- 实现Core.Contracts中定义的仓储接口
- 使用EF Core进行数据库操作
- 处理数据库连接和事务

#### 3.4.2 Caching子层

**职责**: 实现缓存服务，基于Redis。

**组件**: 
- `RedisCacheService.cs`: 实现ICacheService接口，基于StackExchange.Redis

**特点**: 
- 实现Core.Contracts中定义的缓存接口
- 提供Redis缓存实现
- 支持缓存键前缀和过期时间

#### 3.4.3 Configuration子层

**职责**: 管理应用配置，映射appsettings.json文件。

**组件**: 
- `AppSettings.cs`: 应用配置类，映射appsettings.json
- `JwtSettings.cs`: JWT配置类
- `RedisSettings.cs`: Redis配置类

**特点**: 
- 使用Options模式管理配置
- 支持分层配置
- 强类型配置访问

#### 3.4.4 Logging子层

**职责**: 配置和扩展日志功能，基于Serilog。

**组件**: 
- `SerilogExtensions.cs`: Serilog扩展类，提供配置方法

**特点**: 
- 实现结构化日志
- 支持多目标输出
- 便于配置和扩展

### 3.5 API层 (API Layer)

**职责**: 处理HTTP请求，调用应用层服务，返回HTTP响应，依赖所有其他层。

#### 3.5.1 Controllers子层

**职责**: 定义API控制器，处理HTTP请求。

**组件**: 
- `AuthController.cs`: 认证控制器，处理登录、注册和刷新令牌请求
- `UsersController.cs`: 用户控制器，处理用户CRUD操作

**特点**: 
- 使用ASP.NET Core控制器
- 实现RESTful API
- 处理请求验证和授权
- 返回标准化响应

#### 3.5.2 中间件

**职责**: 处理HTTP请求管道中的横切关注点。

**组件**: 
- 异常处理中间件: 统一处理应用异常
- 认证中间件: 处理JWT认证
- 授权中间件: 处理API授权
- HTTPS重定向中间件: 强制HTTPS

#### 3.5.3 启动配置

**职责**: 配置服务和中间件，构建应用。

**组件**: 
- `Program.cs`: 应用入口，配置服务和中间件

**特点**: 
- 使用WebApplicationBuilder构建应用
- 配置依赖注入
- 配置中间件管道

### 3.6 测试层 (Tests Layer)

**职责**: 包含单元测试和集成测试，验证应用功能。

#### 3.6.1 UnitTests子层

**职责**: 测试应用的各个组件，模拟外部依赖。

**特点**: 
- 测试单个组件或类
- 使用Moq等框架模拟依赖
- 运行速度快

#### 3.6.2 IntegrationTests子层

**职责**: 测试应用的集成功能，包括数据库访问和API调用。

**特点**: 
- 测试组件之间的交互
- 使用真实或测试数据库
- 运行速度相对较慢

## 4. 依赖关系与引用规则

### 4.1 依赖方向

**核心原则**: 依赖方向是从外层向内层，即外层依赖内层，内层不依赖外层。

```
API Layer → Application Layer → Core Layer
        ↘           ↘
             ↘           ↘
                   ↘           ↘
                         Infrastructure Layer

Tests Layer → 所有其他层
```

### 4.2 具体引用关系

| 层           | 可以引用的层                | 引用方式                      |
|--------------|---------------------------|-------------------------------|
| Core.Domain  | 无                        | 直接使用                      |
| Core.Contracts | 无                      | 直接使用                      |
| Core.Shared  | 无                        | 直接使用                      |
| Application.DTOs | Core.Domain           | 引用Core.Domain项目           |
| Application.Mapping | Core.Domain, Application.DTOs | 引用Core.Domain和Application.DTOs项目 |
| Application.Services | Core.Contracts, Application.DTOs, Application.Mapping | 引用Core.Contracts和Application项目 |
| Infrastructure.Data | Core.Contracts, Core.Domain | 引用Core项目                 |
| Infrastructure.Caching | Core.Contracts | 引用Core.Contracts项目         |
| Infrastructure.Configuration | 无 | 直接使用                      |
| Infrastructure.Logging | 无 | 直接使用                      |
| API          | Core, Application, Infrastructure | 引用所有项目             |
| Tests        | 所有其他层                | 引用所有项目                  |

### 4.3 依赖注入规则

1. **接口注入**: 所有服务通过接口注入，不直接依赖实现
2. **生命周期管理**: 通过依赖注入容器管理服务生命周期
3. **配置注入**: 配置通过IOptions或直接注入AppSettings对象
4. **仓储注入**: 仓储通过接口注入，便于替换实现
5. **服务注入**: 业务服务通过接口注入，便于测试和替换

## 5. 数据流

### 5.1 API请求处理流程

```
┌───────────────────────────────────────────────────────────────────────────┐
│                              HTTP Request                                │
└───────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌───────────────────────────────────────────────────────────────────────────┐
│                            API Controller                                │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │ 1. 接收请求参数                                               │  │
│  │ 2. 验证请求参数                                               │  │
│  │ 3. 调用应用服务                                               │  │
│  │ 4. 转换返回结果                                               │  │
│  │ 5. 返回HTTP响应                                               │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌───────────────────────────────────────────────────────────────────────────┐
│                          Application Service                             │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │ 1. 实现业务逻辑                                               │  │
│  │ 2. 调用仓储服务                                               │  │
│  │ 3. 调用缓存服务                                               │  │
│  │ 4. 调用其他服务                                               │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌───────────────────────────────────────────────────────────────────────────┐
│                            Infrastructure                               │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────┐  │
│  │      Repository         │  │       Cache           │  │   Database   │  │
│  └─────────────────────────┘  └─────────────────────────┘  └─────────────┘  │
└───────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌───────────────────────────────────────────────────────────────────────────┐
│                              Data Storage                                │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────┐  │
│  │       SQL Server        │  │       Redis           │  │   File System│  │
│  └─────────────────────────┘  └─────────────────────────┘  └─────────────┘  │
└───────────────────────────────────────────────────────────────────────────┘
```

### 5.2 示例：创建用户流程

1. **API层**: UsersController.CreateUser接收UserCreateDto
2. **应用层**: UserService.CreateUserAsync处理业务逻辑
3. **基础设施层**: 
   - UserRepository.AddAsync保存用户到数据库
   - RedisCacheService.RemoveAsync清除用户缓存
4. **数据库**: 保存用户数据
5. **API层**: 返回UserDto和201 Created响应

## 6. 扩展点

### 6.1 替换仓储实现

可以通过实现IRepository<T>接口替换数据库访问实现，例如：
- 使用MongoDB代替SQL Server
- 使用Dapper代替EF Core

### 6.2 替换缓存实现

可以通过实现ICacheService接口替换缓存实现，例如：
- 使用MemoryCache代替Redis
- 使用其他缓存服务

### 6.3 替换日志实现

可以通过配置替换日志实现，例如：
- 使用NLog代替Serilog
- 使用Log4Net代替Serilog

### 6.4 添加新业务模块

添加新业务模块的步骤：
1. 在Core.Domain中定义新实体
2. 在Core.Contracts中定义新接口
3. 在Application.DTOs中定义新DTO
4. 在Application.Mapping中添加映射规则
5. 在Application.Services中实现业务逻辑
6. 在Infrastructure.Data中实现仓储
7. 在API.Controllers中添加新控制器
8. 在Tests中添加测试

## 7. 最佳实践

### 7.1 代码组织

- 遵循按功能模块化组织代码
- 每个模块包含自己的实体、接口、服务和控制器
- 保持类的单一职责

### 7.2 命名约定

- 接口使用I前缀，如IUserService
- 实现类使用具体名称，如UserService
- DTO使用Dto后缀，如UserDto
- 异常使用Exception后缀，如NotFoundException

### 7.3 依赖注入

- 所有服务通过接口注入
- 避免服务定位器模式
- 合理设置服务生命周期

### 7.4 错误处理

- 使用自定义异常表示业务错误
- 在API层统一处理异常
- 返回标准化的错误响应

### 7.5 日志记录

- 在服务层记录业务操作日志
- 在API层记录请求日志
- 在中间件记录异常日志
- 使用结构化日志便于分析

### 7.6 测试

- 为所有业务逻辑编写单元测试
- 为关键流程编写集成测试
- 使用模拟对象隔离测试
- 测试覆盖率不低于80%

## 8. 结论

本文档详细描述了Chet.WebApi.Template项目的分层架构设计，包括各层的职责、组件关系和依赖引用规则。该架构遵循了依赖倒置原则和分层设计原则，实现了高内聚、低耦合，便于维护和扩展。

通过遵循本文档中的设计原则和最佳实践，可以开发出可维护、可扩展、可测试的Web API应用，支持从单体服务到微服务的演进。