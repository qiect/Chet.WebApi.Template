# 数据库设计

## 1. 设计概述

本文档详细描述了Chet.WebApi.Template项目的数据库设计，包括表结构、关系、索引和约束。本设计采用EF Core Code First方法，支持SQLite（开发）和PostgreSQL（生产）数据库。

## 2. 数据库规范

### 2.1 命名规范
- 表名：复数形式，PascalCase（如 Users）
- 列名：PascalCase（如 Id, UserName, Email）
- 外键：关联表名_Id（如 UserId）
- 索引：IX_TableName_ColumnName（如 IX_Users_Email）

### 2.2 数据类型规范
- 主键：int（自动递增）
- 字符串：nvarchar(max) 或指定长度
- 日期时间：datetime2
- 布尔值：bit

## 3. 实体关系图

```
Users
├── Id: int (PK)
├── Name: nvarchar(100) (NN)
├── Email: nvarchar(255) (NN, UK)
├── PasswordHash: nvarchar(max) (NN)
├── CreatedAt: datetime2 (NN)
├── UpdatedAt: datetime2 (NN)
└── IsActive: bit (NN, DF: 1)
```

## 4. 详细表结构

### 4.1 Users 表

| 列名 | 数据类型 | 约束 | 说明 |
|------|----------|------|------|
| Id | int | PK, AutoIncrement | 用户唯一标识 |
| Name | nvarchar(100) | NN | 用户姓名 |
| Email | nvarchar(255) | NN, UK | 用户邮箱（唯一） |
| PasswordHash | nvarchar(max) | NN | 密码哈希值 |
| CreatedAt | datetime2 | NN | 创建时间 |
| UpdatedAt | datetime2 | NN | 更新时间 |
| IsActive | bit | NN, DF: 1 | 是否激活 |

### 4.2 BaseEntity 基类

所有实体继承自BaseEntity类，包含公共字段：

```csharp
public abstract class BaseEntity
{
    public int Id { get; set; }
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime? UpdatedAt { get; set; }
}
```

## 5. EF Core 配置

### 5.1 DbContext 配置

```csharp
public class AppDbContext : DbContext
{
    public AppDbContext(DbContextOptions<AppDbContext> options) : base(options)
    {
    }

    public DbSet<User> Users { get; set; }

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        // 配置User实体
        modelBuilder.Entity<User>(entity =>
        {
            // 表名配置
            entity.ToTable("Users");

            // 主键配置
            entity.HasKey(e => e.Id);

            // 属性配置
            entity.Property(e => e.Name)
                .IsRequired()
                .HasMaxLength(100);

            entity.Property(e => e.Email)
                .IsRequired()
                .HasMaxLength(255);

            entity.Property(e => e.PasswordHash)
                .IsRequired();

            // 索引配置
            entity.HasIndex(e => e.Email)
                .IsUnique();

            // 数据种子
            entity.HasData(
                new User
                {
                    Id = 1,
                    Name = "Admin User",
                    Email = "admin@example.com",
                    PasswordHash = BCrypt.Net.BCrypt.HashPassword("Password123!"),
                    CreatedAt = DateTime.UtcNow,
                    IsActive = true
                }
            );
        });

        base.OnModelCreating(modelBuilder);
    }
}
```

### 5.2 用户实体配置

```csharp
public class User : BaseEntity
{
    public string Name { get; set; } = string.Empty;
    public string Email { get; set; } = string.Empty;
    public string PasswordHash { get; set; } = string.Empty;
    public bool IsActive { get; set; } = true;
    public DateTime? LastLoginAt { get; set; }
}
```

## 6. 数据库迁移

### 6.1 迁移命令

```bash
# 添加新迁移
dotnet ef migrations add InitialCreate --project Chet.WebApi.Template.Data --startup-project Chet.WebApi.Template.Api

# 更新数据库
dotnet ef database update --project Chet.WebApi.Template.Data --startup-project Chet.WebApi.Template.Api

# 查看挂起的迁移
dotnet ef migrations list --project Chet.WebApi.Template.Data --startup-project Chet.WebApi.Template.Api

# 回滚迁移
dotnet ef migrations remove --project Chet.WebApi.Template.Data --startup-project Chet.WebApi.Template.Api
```

### 6.2 迁移文件结构

迁移文件位于 `Chet.WebApi.Template.Data/Migrations/` 目录下：

```
Migrations/
├── 20231027103000_InitialCreate.cs
├── 20231027103000_InitialCreate.Designer.cs
└── AppDbContextModelSnapshot.cs
```

## 7. 数据库配置

### 7.1 连接字符串配置

在 `appsettings.json` 中配置数据库连接：

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=ChetWebApiTemplate.db",
    "PostgreSqlConnection": "Host=localhost;Database=chet_webapi;Username=postgres;Password=password"
  }
}
```

### 7.2 数据库上下文注册

在 `Program.cs` 中注册数据库上下文：

```csharp
// 注册数据库上下文
builder.Services.AddDbContext<AppDbContext>(options =>
{
    if (builder.Environment.IsDevelopment())
    {
        options.UseSqlite(builder.Configuration.GetConnectionString("DefaultConnection"));
    }
    else
    {
        options.UseNpgsql(builder.Configuration.GetConnectionString("PostgreSqlConnection"));
    }
});
```

## 8. 仓储模式实现

### 8.1 通用仓储接口

```csharp
public interface IRepository<T> where T : BaseEntity
{
    Task<T?> GetByIdAsync(int id);
    Task<IEnumerable<T>> GetAllAsync();
    Task<T> AddAsync(T entity);
    Task<T> UpdateAsync(T entity);
    Task<bool> DeleteAsync(int id);
    Task SaveChangesAsync();
}
```

### 8.2 通用仓储实现

```csharp
public class EfCoreRepository<T> : IRepository<T> where T : BaseEntity
{
    protected readonly AppDbContext _context;
    protected readonly DbSet<T> _dbSet;

    public EfCoreRepository(AppDbContext context)
    {
        _context = context;
        _dbSet = context.Set<T>();
    }

    public virtual async Task<T?> GetByIdAsync(int id)
    {
        return await _dbSet.FindAsync(id);
    }

    public virtual async Task<IEnumerable<T>> GetAllAsync()
    {
        return await _dbSet.ToListAsync();
    }

    public virtual async Task<T> AddAsync(T entity)
    {
        entity.CreatedAt = DateTime.UtcNow;
        _ = await _dbSet.AddAsync(entity);
        return entity;
    }

    public virtual async Task<T> UpdateAsync(T entity)
    {
        entity.UpdatedAt = DateTime.UtcNow;
        _ = _dbSet.Update(entity);
        return entity;
    }

    public virtual async Task<bool> DeleteAsync(int id)
    {
        var entity = await _dbSet.FindAsync(id);
        if (entity == null) return false;

        _ = _dbSet.Remove(entity);
        return true;
    }

    public virtual async Task SaveChangesAsync()
    {
        await _context.SaveChangesAsync();
    }
}
```

### 8.3 用户仓储实现

```csharp
public class UserRepository : EfCoreRepository<User>, IUserRepository
{
    public UserRepository(AppDbContext context) : base(context)
    {
    }

    public async Task<User?> GetByEmailAsync(string email)
    {
        return await _dbSet.FirstOrDefaultAsync(u => u.Email.ToLower() == email.ToLower());
    }

    public async Task<IEnumerable<User>> GetActiveUsersAsync()
    {
        return await _dbSet.Where(u => u.IsActive).ToListAsync();
    }
}
```

## 9. 性能优化

### 9.1 索引策略

- 为经常查询的列创建索引
- 为外键列创建索引
- 为唯一约束列创建唯一索引

### 9.2 查询优化

- 使用投影减少数据传输 (`Select`)
- 使用 `AsNoTracking()` 优化只读查询
- 避免 N+1 查询问题

```csharp
// 优化查询示例
public async Task<IEnumerable<UserDto>> GetUsersWithPaginationAsync(int page, int pageSize)
{
    return await _context.Users
        .AsNoTracking()  // 对于只读查询使用AsNoTracking
        .Skip((page - 1) * pageSize)
        .Take(pageSize)
        .Select(u => new UserDto  // 使用投影只获取需要的字段
        {
            Id = u.Id,
            Name = u.Name,
            Email = u.Email,
            IsActive = u.IsActive
        })
        .ToListAsync();
}
```

## 10. 安全考虑

### 10.1 数据保护

- 密码必须使用强哈希算法（如BCrypt）
- 敏感数据应加密存储
- 实施适当的访问控制

### 10.2 SQL注入防护

- 使用参数化查询
- 避免动态SQL拼接
- 使用EF Core的LINQ查询

## 11. 数据库维护

### 11.1 备份策略

- 定期备份数据库
- 测试备份恢复流程
- 保留多个时间点的备份

### 11.2 数据清理

- 定期清理过期数据
- 归档历史数据
- 监控数据库大小

## 12. 扩展性考虑

### 12.1 分库分表

当数据量增长时，可考虑：

- 按时间分区
- 按用户ID哈希分片
- 读写分离

### 12.2 缓存策略

- 使用Redis缓存热点数据
- 实现缓存失效策略
- 考虑缓存穿透防护

通过遵循本文档的设计规范和最佳实践，可以确保数据库的稳定性、性能和可维护性。