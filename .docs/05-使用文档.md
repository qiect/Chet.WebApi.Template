# 使用文档

## 1. 概述

本文档详细描述了Chet.WebApi.Template项目中提供的API端点，包括请求方式、参数、响应格式和使用示例。本项目采用RESTful API设计风格，使用JSON作为数据交换格式。

## 2. API基础信息

### 2.1 基础URL

- 开发环境: `http://localhost:5000/api`
- 生产环境: `https://yourdomain.com/api`

### 2.2 认证方式

API使用JWT（JSON Web Token）进行身份验证。请求时需在HTTP头部添加Authorization字段：

```
Authorization: Bearer {token}
```

### 2.3 内容类型

- 请求: `application/json`
- 响应: `application/json`

### 2.4 响应格式

所有API响应都遵循统一的响应模型：

```csharp
public class ApiResponse<T>
{
    public int StatusCode { get; set; }
    public bool Success { get; set; }
    public string? Message { get; set; }
    public T? Data { get; set; }
    public DateTime Timestamp { get; set; }
}
```

## 3. 用户管理API

### 3.1 用户认证

#### 3.1.1 登录

- **URL**: `/api/auth/login`
- **方法**: `POST`
- **认证**: 不需要
- **内容类型**: `application/json`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| email | string | 是 | 用户邮箱 |
| password | string | 是 | 用户密码 |

**请求示例**:

```json
{
  "email": "user@example.com",
  "password": "Password123!"
}
```

**成功响应**:

```json
{
  "statusCode": 200,
  "success": true,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "name": "John Doe",
      "email": "user@example.com",
      "isActive": true
    }
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

**错误响应**:

```json
{
  "statusCode": 401,
  "success": false,
  "message": "无效的凭据",
  "data": null,
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 3.1.2 注册

- **URL**: `/api/auth/register`
- **方法**: `POST`
- **认证**: 不需要
- **内容类型**: `application/json`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 用户姓名 |
| email | string | 是 | 用户邮箱 |
| password | string | 是 | 用户密码 |

**请求示例**:

```json
{
  "name": "Jane Smith",
  "email": "jane@example.com",
  "password": "Password123!"
}
```

**成功响应**:

```json
{
  "statusCode": 201,
  "success": true,
  "message": "用户注册成功",
  "data": {
    "id": 2,
    "name": "Jane Smith",
    "email": "jane@example.com",
    "isActive": true
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

### 3.2 用户操作

#### 3.2.1 获取当前用户信息

- **URL**: `/api/users/me`
- **方法**: `GET`
- **认证**: 需要JWT Token
- **内容类型**: `application/json`

**请求头**:

```
Authorization: Bearer {token}
```

**成功响应**:

```json
{
  "statusCode": 200,
  "success": true,
  "message": "操作成功",
  "data": {
    "id": 1,
    "name": "John Doe",
    "email": "user@example.com",
    "isActive": true,
    "createdAt": "2023-10-01T10:00:00Z"
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 3.2.2 获取用户列表

- **URL**: `/api/users`
- **方法**: `GET`
- **认证**: 需要JWT Token
- **内容类型**: `application/json`

**查询参数**:

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 10 | 每页数量 |
| isActive | boolean | 否 | null | 是否活跃 |

**请求示例**:

```
GET /api/users?page=1&pageSize=5&isActive=true
Authorization: Bearer {token}
```

**成功响应**:

```json
{
  "statusCode": 200,
  "success": true,
  "message": "操作成功",
  "data": {
    "items": [
      {
        "id": 1,
        "name": "John Doe",
        "email": "user@example.com",
        "isActive": true,
        "createdAt": "2023-10-01T10:00:00Z"
      },
      {
        "id": 2,
        "name": "Jane Smith",
        "email": "jane@example.com",
        "isActive": true,
        "createdAt": "2023-10-02T15:30:00Z"
      }
    ],
    "total": 25,
    "page": 1,
    "pageSize": 5,
    "totalPages": 5
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 3.2.3 获取单个用户

- **URL**: `/api/users/{id}`
- **方法**: `GET`
- **认证**: 需要JWT Token
- **内容类型**: `application/json`

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | integer | 是 | 用户ID |

**请求示例**:

```
GET /api/users/1
Authorization: Bearer {token}
```

**成功响应**:

```json
{
  "statusCode": 200,
  "success": true,
  "message": "操作成功",
  "data": {
    "id": 1,
    "name": "John Doe",
    "email": "user@example.com",
    "isActive": true,
    "createdAt": "2023-10-01T10:00:00Z",
    "updatedAt": "2023-10-20T14:20:00Z"
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

**错误响应**:

```json
{
  "statusCode": 404,
  "success": false,
  "message": "用户不存在",
  "data": null,
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 3.2.4 创建用户

- **URL**: `/api/users`
- **方法**: `POST`
- **认证**: 需要JWT Token (需要管理员权限)
- **内容类型**: `application/json`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 用户姓名 |
| email | string | 是 | 用户邮箱 |
| password | string | 是 | 用户密码 |

**请求示例**:

```json
{
  "name": "New User",
  "email": "newuser@example.com",
  "password": "Password123!"
}
```

**成功响应**:

```json
{
  "statusCode": 201,
  "success": true,
  "message": "用户创建成功",
  "data": {
    "id": 3,
    "name": "New User",
    "email": "newuser@example.com",
    "isActive": true,
    "createdAt": "2023-10-27T10:30:00Z"
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 3.2.5 更新用户

- **URL**: `/api/users/{id}`
- **方法**: `PUT`
- **认证**: 需要JWT Token
- **内容类型**: `application/json`

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | integer | 是 | 用户ID |

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 用户姓名 |
| email | string | 否 | 用户邮箱 |

**请求示例**:

```json
{
  "name": "Updated Name",
  "email": "updated@example.com"
}
```

**成功响应**:

```json
{
  "statusCode": 200,
  "success": true,
  "message": "用户更新成功",
  "data": {
    "id": 1,
    "name": "Updated Name",
    "email": "updated@example.com",
    "isActive": true,
    "createdAt": "2023-10-01T10:00:00Z",
    "updatedAt": "2023-10-27T10:30:00Z"
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 3.2.6 删除用户

- **URL**: `/api/users/{id}`
- **方法**: `DELETE`
- **认证**: 需要JWT Token (需要管理员权限)
- **内容类型**: `application/json`

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | integer | 是 | 用户ID |

**请求示例**:

```
DELETE /api/users/1
Authorization: Bearer {token}
```

**成功响应**:

```json
{
  "statusCode": 200,
  "success": true,
  "message": "用户删除成功",
  "data": null,
  "timestamp": "2023-10-27T10:30:00Z"
}
```

## 4. 错误处理

### 4.1 HTTP状态码

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 请求错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 409 | 冲突（如邮箱已存在） |
| 500 | 服务器内部错误 |

### 4.2 错误响应格式

```json
{
  "statusCode": 400,
  "success": false,
  "message": "请求参数错误",
  "data": {
    "errors": [
      {
        "field": "email",
        "message": "邮箱格式不正确"
      },
      {
        "field": "password",
        "message": "密码长度不能少于8个字符"
      }
    ]
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

### 4.3 常见错误示例

#### 4.3.1 参数验证错误

```json
{
  "statusCode": 400,
  "success": false,
  "message": "输入验证失败",
  "data": {
    "errors": [
      {
        "field": "email",
        "message": "邮箱是必需的"
      }
    ]
  },
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 4.3.2 未授权错误

```json
{
  "statusCode": 401,
  "success": false,
  "message": "未授权访问",
  "data": null,
  "timestamp": "2023-10-27T10:30:00Z"
}
```

#### 4.3.3 资源冲突错误

```json
{
  "statusCode": 409,
  "success": false,
  "message": "邮箱已被使用",
  "data": null,
  "timestamp": "2023-10-27T10:30:00Z"
}
```

## 5. 分页响应格式

当API返回分页数据时，使用以下格式：

```csharp
public class PaginatedResponse<T>
{
    public IEnumerable<T> Items { get; set; } = Array.Empty<T>();
    public int Total { get; set; }
    public int Page { get; set; }
    public int PageSize { get; set; }
    public int TotalPages { get; set; }
    public bool HasPreviousPage => Page > 1;
    public bool HasNextPage => Page < TotalPages;
}
```

## 6. API使用示例

### 6.1 JavaScript/TypeScript客户端示例

```typescript
class ApiClient {
  private baseUrl: string;
  private token: string | null;

  constructor(baseUrl: string) {
    this.baseUrl = baseUrl;
    this.token = localStorage.getItem('token');
  }

  private async request<T>(endpoint: string, options: RequestInit = {}): Promise<T> {
    const url = `${this.baseUrl}${endpoint}`;
    const headers = {
      'Content-Type': 'application/json',
      ...(this.token && { 'Authorization': `Bearer ${this.token}` }),
      ...options.headers
    };

    const response = await fetch(url, { ...options, headers });
    const data = await response.json();

    if (!response.ok) {
      throw new Error(data.message || '请求失败');
    }

    return data;
  }

  async login(email: string, password: string) {
    const response = await this.request('/auth/login', {
      method: 'POST',
      body: JSON.stringify({ email, password })
    });

    this.token = response.data.token;
    localStorage.setItem('token', this.token);
    return response;
  }

  async getUsers(page: number = 1, pageSize: number = 10) {
    return await this.request(`/users?page=${page}&pageSize=${pageSize}`);
  }

  async getUserById(id: number) {
    return await this.request(`/users/${id}`);
  }
}
```

### 6.2 C#客户端示例

```csharp
public class ApiClient
{
    private readonly HttpClient _httpClient;
    private readonly string _baseUrl;

    public ApiClient(HttpClient httpClient, string baseUrl)
    {
        _httpClient = httpClient;
        _baseUrl = baseUrl;
    }

    public async Task<ApiResponse<AuthResponse>> LoginAsync(LoginRequest request)
    {
        var json = JsonSerializer.Serialize(request);
        var content = new StringContent(json, Encoding.UTF8, "application/json");

        var response = await _httpClient.PostAsync($"{_baseUrl}/auth/login", content);
        var responseContent = await response.Content.ReadAsStringAsync();

        return JsonSerializer.Deserialize<ApiResponse<AuthResponse>>(responseContent);
    }

    public async Task<ApiResponse<PaginatedResponse<UserDto>>> GetUsersAsync(
        int page = 1, 
        int pageSize = 10, 
        bool? isActive = null)
    {
        var queryString = $"?page={page}&pageSize={pageSize}";
        if (isActive.HasValue)
        {
            queryString += $"&isActive={isActive.Value}";
        }

        var request = new HttpRequestMessage(HttpMethod.Get, $"{_baseUrl}/users{queryString}");
        request.Headers.Authorization = new AuthenticationHeaderValue("Bearer", GetToken());

        var response = await _httpClient.SendAsync(request);
        var responseContent = await response.Content.ReadAsStringAsync();

        return JsonSerializer.Deserialize<ApiResponse<PaginatedResponse<UserDto>>>(responseContent);
    }

    private string GetToken()
    {
        // 从配置或存储中获取JWT token
        return "your-jwt-token-here";
    }
}

public class LoginRequest
{
    public string Email { get; set; } = string.Empty;
    public string Password { get; set; } = string.Empty;
}

public class AuthResponse
{
    public string Token { get; set; } = string.Empty;
    public UserDto User { get; set; } = new UserDto();
}
```

## 7. API版本管理

### 7.1 版本控制策略

本API采用URL路径版本控制：

- `/api/v1/users` - 第1版API
- `/api/v2/users` - 第2版API

### 7.2 版本声明

在 `Program.cs` 中配置API版本：

```csharp
builder.Services.AddApiVersioning(options =>
{
    options.AssumeDefaultVersionWhenUnspecified = true;
    options.DefaultApiVersion = new ApiVersion(1, 0);
    options.ReportApiVersions = true;
});
```

## 8. API安全注意事项

### 8.1 认证与授权

- 所有敏感操作都需要有效的JWT Token
- 使用HTTPS协议确保传输安全
- 定期刷新JWT Token

### 8.2 输入验证

- 所有输入参数都经过严格验证
- 防止SQL注入和XSS攻击
- 使用参数化查询

### 8.3 速率限制

- 实施API速率限制防止滥用
- 单个IP地址限制请求频率

## 9. SDK和工具

### 9.1 Swagger/OpenAPI

API文档通过Swagger UI提供，访问地址：`http://localhost:5000/swagger`

### 9.2 Postman集合

项目根目录下提供了Postman集合文件，可用于API测试。

通过以上API文档，开发者可以快速了解和使用本项目提供的API端点，实现与后端服务的交互。