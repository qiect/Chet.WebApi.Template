# 安全设计

## 1. 安全概述

本文档详细描述了Chet.WebApi.Template项目的安全设计，包括身份验证、授权、数据保护和安全最佳实践。本设计采用JWT（JSON Web Token）作为主要的身份验证机制，结合多种安全措施确保API的安全性。

## 2. 身份验证机制

### 2.1 JWT认证流程

```
客户端请求 -> 验证凭据 -> 生成JWT -> 返回令牌 -> 后续请求携带令牌 -> 验证令牌 -> 授权访问
```

### 2.2 JWT配置

在 `appsettings.json` 中配置JWT参数：

```json
{
  "JwtSettings": {
    "Secret": "YourSuperSecretKeyThatShouldBeAtLeast32CharactersLong",
    "Issuer": "Chet.WebApi.Template",
    "Audience": "Chet.WebApi.Template.Audience",
    "TokenExpirationInMinutes": 60
  }
}
```

### 2.3 JWT实现

```csharp
public class JwtService
{
    private readonly IConfiguration _configuration;

    public JwtService(IConfiguration configuration)
    {
        _configuration = configuration;
    }

    public string GenerateToken(User user)
    {
        var secret = _configuration["JwtSettings:Secret"];
        var issuer = _configuration["JwtSettings:Issuer"];
        var audience = _configuration["JwtSettings:Audience"];
        var expirationInMinutes = int.Parse(_configuration["JwtSettings:TokenExpirationInMinutes"]);

        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(secret));
        var credentials = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);

        var claims = new[]
        {
            new Claim(ClaimTypes.NameIdentifier, user.Id.ToString()),
            new Claim(ClaimTypes.Name, user.Name),
            new Claim(ClaimTypes.Email, user.Email),
            new Claim(JwtRegisteredClaimNames.Jti, Guid.NewGuid().ToString()),
            new Claim(JwtRegisteredClaimNames.Iat, DateTimeOffset.UtcNow.ToUnixTimeSeconds().ToString(), ClaimValueTypes.Integer64)
        };

        var token = new JwtSecurityToken(
            issuer: issuer,
            audience: audience,
            claims: claims,
            expires: DateTime.UtcNow.AddMinutes(expirationInMinutes),
            signingCredentials: credentials);

        return new JwtSecurityTokenHandler().WriteToken(token);
    }

    public ClaimsPrincipal ValidateToken(string token)
    {
        var secret = _configuration["JwtSettings:Secret"];
        var issuer = _configuration["JwtSettings:Issuer"];
        var audience = _configuration["JwtSettings:Audience"];

        var tokenHandler = new JwtSecurityTokenHandler();
        var key = Encoding.UTF8.GetBytes(secret);

        var validationParameters = new TokenValidationParameters
        {
            ClockSkew = TimeSpan.Zero, // 默认: 5 min
            ValidateIssuer = true,
            ValidateAudience = true,
            ValidateLifetime = true,
            ValidateIssuerSigningKey = true,
            ValidIssuer = issuer,
            ValidAudience = audience,
            IssuerSigningKey = new SymmetricSecurityKey(key)
        };

        return tokenHandler.ValidateToken(token, validationParameters, out _);
    }
}
```

## 3. 授权机制

### 3.1 角色基础授权

```csharp
[Authorize(Roles = "Admin,User")]
[HttpGet]
public async Task<ActionResult<IEnumerable<UserDto>>> GetUsers()
{
    // 只有Admin和User角色可以访问
}

[Authorize(Roles = "Admin")]
[HttpPost]
public async Task<ActionResult<UserDto>> CreateUser(CreateUserDto createUserDto)
{
    // 只有Admin角色可以访问
}
```

### 3.2 策略基础授权

```csharp
// 在Program.cs中配置授权策略
builder.Services.AddAuthorization(options =>
{
    options.AddPolicy("RequireAdministratorRole", policy => policy.RequireRole("Admin"));
    options.AddPolicy("RequireActiveUser", policy => 
        policy.RequireAuthenticatedUser()
              .RequireClaim("IsActive", "true"));
    options.AddPolicy("MinimumAgeRequirement", policy =>
        policy.RequireClaim("Age")
              .RequireAssertion(context =>
              {
                  var ageClaim = context.User.FindFirst(c => c.Type == "Age");
                  if (ageClaim == null) return false;
                  var age = int.Parse(ageClaim.Value);
                  return age >= 18;
              }));
});
```

## 4. 密码安全

### 4.1 密码哈希

使用BCrypt进行密码哈希：

```csharp
public class PasswordService
{
    public static string HashPassword(string password)
    {
        // 生成盐并哈希密码
        return BCrypt.Net.BCrypt.HashPassword(password, BCrypt.Net.BCrypt.GenerateSalt(12));
    }

    public static bool VerifyPassword(string password, string hash)
    {
        // 验证密码
        return BCrypt.Net.BCrypt.Verify(password, hash);
    }
}
```

### 4.2 密码强度验证

```csharp
public static class PasswordValidator
{
    public static bool IsValidPassword(string password)
    {
        // 验证密码至少8个字符，包含大写字母、小写字母、数字和特殊字符
        var hasMinLength = password.Length >= 8;
        var hasUpperCase = Regex.IsMatch(password, @"[A-Z]");
        var hasLowerCase = Regex.IsMatch(password, @"[a-z]");
        var hasNumbers = Regex.IsMatch(password, @"\d");
        var hasSpecialChar = Regex.IsMatch(password, @"[^a-zA-Z0-9]");

        return hasMinLength && hasUpperCase && hasLowerCase && hasNumbers && hasSpecialChar;
    }
}
```

## 5. API安全措施

### 5.1 CORS配置

```csharp
// 在Program.cs中配置CORS
builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowSpecificOrigins", policy =>
    {
        policy.WithOrigins("https://yourdomain.com", "http://localhost:3000") // 替换为实际允许的域名
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials(); // 如果需要发送cookies
    });
});

// 应用CORS中间件
app.UseCors("AllowSpecificOrigins");
```

### 5.2 HTTPS强制

```csharp
// 在开发环境中重定向到HTTPS
if (!app.Environment.IsDevelopment())
{
    app.UseExceptionHandler("/Error");
    app.UseHsts();
    app.UseHttpsRedirection();
}
```

### 5.3 抗请求伪造（Anti-Forgery）

```csharp
// 在Program.cs中注册Anti-Forgery服务
builder.Services.AddAntiforgery(options =>
{
    options.HeaderName = "X-XSRF-TOKEN";
    options.SuppressXFrameOptionsHeader = false;
});
```

## 6. 输入验证与清理

### 6.1 模型验证

```csharp
public class CreateUserDto
{
    [Required(ErrorMessage = "用户名不能为空")]
    [StringLength(100, ErrorMessage = "用户名不能超过100个字符")]
    [RegularExpression(@"^[a-zA-Z0-9_]+$", ErrorMessage = "用户名只能包含字母、数字和下划线")]
    public string Name { get; set; } = string.Empty;

    [Required(ErrorMessage = "邮箱不能为空")]
    [EmailAddress(ErrorMessage = "邮箱格式不正确")]
    public string Email { get; set; } = string.Empty;

    [Required(ErrorMessage = "密码不能为空")]
    [StringLength(100, MinimumLength = 8, ErrorMessage = "密码长度必须在8-100个字符之间")]
    public string Password { get; set; } = string.Empty;
}
```

### 6.2 输出编码

```csharp
// 使用输出编码防止XSS攻击
public class SafeOutputService
{
    public static string HtmlEncode(string input)
    {
        return System.Net.WebUtility.HtmlEncode(input);
    }

    public static string JavaScriptEncode(string input)
    {
        return System.Text.Json.JsonSerializer.Serialize(input);
    }
}
```

## 7. 安全头设置

### 7.1 安全头中间件

```csharp
// 自定义安全头中间件
public class SecurityHeadersMiddleware
{
    private readonly RequestDelegate _next;

    public SecurityHeadersMiddleware(RequestDelegate next)
    {
        _next = next;
    }

    public async Task InvokeAsync(HttpContext context)
    {
        // 设置安全头
        context.Response.Headers.Add("X-Content-Type-Options", "nosniff");
        context.Response.Headers.Add("X-Frame-Options", "DENY");
        context.Response.Headers.Add("X-XSS-Protection", "1; mode=block");
        context.Response.Headers.Add("Referrer-Policy", "strict-origin-when-cross-origin");
        context.Response.Headers.Add("Permissions-Policy", "geolocation=(), microphone=(), camera=()");
        
        await _next(context);
    }
}

// 在Program.cs中注册中间件
app.UseMiddleware<SecurityHeadersMiddleware>();
```

## 8. 速率限制

### 8.1 速率限制配置

```csharp
// 在Program.cs中配置速率限制
builder.Services.AddRateLimiter(options =>
{
    options.GlobalLimiter = PartitionedLimiter.Create<HttpContext, string>((_, _) =>
        SlidingWindowPartitionedRateLimiterOptions.Create(
            permitLimit: 100,
            window: TimeSpan.FromMinutes(1),
            segments: 1,
            queueLimit: 0,
            queueProcessingOrder: QueueProcessingOrder.OldestFirst));

    // IP地址限流
    options.AddPartitionedLimiter<HttpContext, string>("IpRateLimiter", context =>
        TypedLimiter.Create(
            new[] { "Ip:" + context.Connection.RemoteIpAddress?.ToString() ?? "unknown" },
            partitionedOptions: SlidingWindowPartitionedRateLimiterOptions.Create(
                permitLimit: 10,
                window: TimeSpan.FromMinutes(1),
                segments: 1,
                queueLimit: 0,
                queueProcessingOrder: QueueProcessingOrder.OldestFirst)));
});
```

## 9. 日志安全

### 9.1 敏感信息过滤

```csharp
// 在Serilog配置中过滤敏感信息
{
  "Serilog": {
    "Filter": [
      {
        "Name": "With",
        "Args": {
          "enricher": {
            "Name": "MaskProperties",
            "Args": {
              "properties": ["Password", "Token", "Authorization"],
              "mask": "***"
            }
          }
        }
      }
    ]
  }
}
```

## 10. 安全最佳实践

### 10.1 安全检查清单

- [ ] 使用HTTPS协议
- [ ] 实施适当的身份验证和授权
- [ ] 对密码进行适当哈希
- [ ] 验证和清理所有输入
- [ ] 防止常见的Web漏洞（XSS、CSRF、SQL注入等）
- [ ] 正确配置CORS
- [ ] 设置适当的安全头
- [ ] 实施速率限制
- [ ] 记录安全相关事件
- [ ] 定期更新依赖项
- [ ] 使用最新的安全标准和协议

### 10.2 安全事件监控

```csharp
public class SecurityEventService
{
    private readonly ILogger<SecurityEventService> _logger;

    public SecurityEventService(ILogger<SecurityEventService> logger)
    {
        _logger = logger;
    }

    public void LogFailedLoginAttempt(string username, string ipAddress)
    {
        _logger.LogWarning("登录失败 - 用户名: {Username}, IP地址: {IpAddress}", username, ipAddress);
    }

    public void LogSuspiciousActivity(string activity, string userId, string ipAddress)
    {
        _logger.LogError("可疑活动 - 活动: {Activity}, 用户ID: {UserId}, IP地址: {IpAddress}", 
            activity, userId, ipAddress);
    }

    public void LogSuccessfulLogin(string username, string userId, string ipAddress)
    {
        _logger.LogInformation("登录成功 - 用户名: {Username}, 用户ID: {UserId}, IP地址: {IpAddress}", 
            username, userId, ipAddress);
    }
}
```

## 11. 常见安全漏洞防护

### 11.1 SQL注入防护

- 使用参数化查询
- 使用ORM（如EF Core）的LINQ查询
- 避免动态SQL拼接

### 11.2 XSS防护

- 验证和清理用户输入
- 在输出时进行编码
- 设置适当的CSP头

### 11.3 CSRF防护

- 使用防伪令牌
- 验证请求来源
- 对敏感操作使用额外验证

通过实施以上安全措施，可以有效保护API免受各种安全威胁，确保系统的安全性。